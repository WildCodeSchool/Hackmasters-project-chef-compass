export const environment = {
    //Set production environment to true or false based on environment needed.
    //Don't forget to name the file accordingly i.e:
    //environment.ts or environment.prod.ts
    //These files will be then automatically gitignored once named correctly*
    
    production: false,
    apiKey: ''
}

